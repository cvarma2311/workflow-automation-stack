# Orchestration
prefect==2.19.6

# Spark + Iceberg
pyspark==3.5.1
pyiceberg==0.6.1  # Optional for Python direct Iceberg API usage

# Storage
minio==7.2.7      # MinIO Python client (if needed for S3 ops in Python)
boto3==1.34.162   # AWS S3 SDK, works with MinIO

# Database connectivity
psycopg2-binary==2.9.9  # PostgreSQL connector for Iceberg catalog
SQLAlchemy==2.0.31      # For Python DB operations

# Utilities
pandas==2.2.2
pyarrow==16.1.0         # Needed for Arrow data interchange & Iceberg parquet ops

# Security / TLS helpers
certifi==2024.7.4
